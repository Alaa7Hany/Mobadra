Task5:

-spring petclinic:
	first we need to install java17 or higher then we need to install maven

git clone https://github.com/spring-projects/spring-petclinic.git
cd spring-petclinic
./mvnw package
java -jar target/*.jar


-upload all previous tasks to github