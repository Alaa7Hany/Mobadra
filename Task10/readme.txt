Task10:

Loadbalancer on 5 containers spring-petclinic using nginx

-inside the docker-compose.yaml file add a service for nginx
-add deploy to the app service and give it 5 replicas
-replace the default.conf file to enable loadbalancing
