Task1:

Consists of interacting with some basic Linux commands

-make a new directory  "mkdir newfolder"
-make new files  "touch newfile1 newfile2"
-edit the file  "nano newfile1"
-remove a file  "rm newfile2"